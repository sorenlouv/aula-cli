node_modules/
bun.lock

# Vendored from the fleet root by ../scripts/sync-contract.sh. `bun run format`
# reflows its arrays, and the contract test compares parsed JSON, so the rewrite
# passes every check while leaving this copy textually diverged from the root it
# is supposed to mirror — churn that reappears on the next sync.
contract.json

# Gitignored working data — attachments and anything derived from real Aula
# responses. Never formatted, never read.
data/
attachments/

# Git worktrees checked out inside the repo.
.claude/worktrees/

# The docs are hand-wrapped prose with hand-aligned tables. Prettier pads every
# table cell to the widest row and rewrites *emphasis* to _emphasis_, which is
# churn without a payoff. Ignoring them here keeps format-on-save a no-op on
# markdown, so the editor and the agent agree to leave these alone.
*.md
